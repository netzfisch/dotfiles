#!/bin/sh

echo "### install own linux packages ..."
echo "##################################"
sudo apt install --assume-yes aptitude httpie mc openjdk-8-jre postgresql-contrib libpq-dev libsqlite3-dev shellcheck

echo "### install own ruby gems ..."
echo "#############################"
gem install foreman teamocil tldrb

brew bundle --file=- <<EOF
# nothing
EOF

if [ -r "$HOME/.rcrc" ]; then
  echo "### updating dotfiles ..."
  echo "#########################"
  rcup
fi
