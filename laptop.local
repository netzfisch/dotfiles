#!/bin/sh

echo "### install own linux packages ..."
echo "##################################"
sudo apt install --assume-yes aptitude httpie libsqlite3-dev openjdk-8-jre mc shellcheck

echo "### install own ruby gems ..."
echo "#############################"
gem install foreman teamocil tldrb

brew bundle --file=- <<EOF
# nothing
EOF

if [ -r "$HOME/.rcrc" ]; then
  echo "### updating dotfiles ..."
  echo "#########################"
  rcup
fi
